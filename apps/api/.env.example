# MongoDB connection string (we’ll fill this later with your Atlas URI)
NUDO_MONGO_URI=

# App secrets (temporary values are fine locally; we’ll rotate later)
NUDO_JWT_SECRET=dev-change-me
NUDO_INVITE_SIGNING_KEY=dev-change-me

# Timezone for weekly resets
NUDO_TIMEZONE=America/Phoenix